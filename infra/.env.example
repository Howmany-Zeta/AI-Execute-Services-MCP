# 统一测试基础设施 - 环境变量
# 复制为 .env 并修改: cp .env.example .env

# ========== Redis（与 redis_cache 一致）==========
REDIS_PASSWORD=sdfh673uew

# ========== DocumentServer / OnlyOffice ==========
# 预构建镜像（与 DocumentServer/docker-compose 默认一致）
DOCUMENTSERVER_IMAGE=us-central1-docker.pkg.dev/ca-biz-kjmsdw-y59m/aiecs-mcp-servers/aiecs-documentserver:latest

JWT_SECRET=your-jwt-secret-change-me
ONLYOFFICE_PG_PASSWORD=onlyoffice-secret
RABBITMQ_USER=onlyoffice
RABBITMQ_PASSWORD=onlyoffice-rmq-secret
